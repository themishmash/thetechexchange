<%= render "/pages/navbar" %>

<div class="listing-hero">
<h1>Type of Machine</h1>
</div>

<div class="machine-table">
  <div class="machine-table2">
<table id="listing">
<thead>
  <tr>
    <th>Name of machine</th>
    <th colspan="3"></th>
  </tr>
</thead>
<tbody>

<% @machines.each do |machine|%>
  <tr class="listing-row">
    <td><%= machine.name %></td>
  <td><%= link_to 'Edit', edit_machine_path(machine), class:"listing-link" %></td>
    <td><%= link_to 'Destroy', machine_path(machine),
            method: :delete,
            data: { confirm: 'Are you sure sure?'}, class:"listing-link" %></td>

<% end %>

</tbody>
</table>
<br>
<%= link_to 'Add new machine', new_machine_path, class:"blue-button-table" %>

<br>


  </div>
</div>

<%= render 'pages/footer' %>


